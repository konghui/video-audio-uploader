services:
  audio-uploader:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/config/config.yaml:ro
      - ./data/tmp:/data/tmp
      - ./data/baidupcs:/root/.config/BaiduPCS-Go
    restart: unless-stopped
